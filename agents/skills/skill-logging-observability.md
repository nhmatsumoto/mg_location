# skill-logging-observability
- Log início/fim de endpoint crítico com duração e contagens.
- Health endpoint obrigatório (`/api/health`).
- Em erro de integração: payload amigável + log técnico no backend.
