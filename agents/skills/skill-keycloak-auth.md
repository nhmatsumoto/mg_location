# skill-keycloak-auth
- Realm: `sos-location`.
- Client FE: `sos-location-frontend` (public + PKCE).
- Roles base: `volunteer`, `coordinator`, `admin`.
- Guard de rotas privadas no frontend e validação JWT no backend.
